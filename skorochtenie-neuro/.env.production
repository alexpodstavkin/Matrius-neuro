# URL обработчика формы (создаёт сделку в GC).
#
# По умолчанию (если переменная пуста или не задана) фронт шлёт на
#   `${BASE_URL}php/submit.php`  →  `/skorochtenie-neuro/php/submit.php`
# на том же домене (см. skorochtenie-neuro/php/).
#
# Переопределите, если обработчик живёт на другом хосте/пути:
#   VITE_GC_PROXY_URL=https://matrius-gc-api.onrender.com/api/booking
#   VITE_GC_PROXY_URL=https://matrius-gc-proxy.YOUR-ACCOUNT.workers.dev
VITE_GC_PROXY_URL=
